import { useState } from 'react'
import './ProjectAccordion.css'
export default function ProjectAccordion({ projects }) { const [active, setActive] = useState(0); const activate = (index) => setActive(index); const handleKeyDown = (event, index) => { if (event.key === 'Enter' || event.key === ' ') { event.preventDefault(); activate(index); } }; return <div className="project-accordion" role="list" aria-label="项目图层">{projects.map((project, index) => <article role="listitem" className={`project-panel ${active === index ? 'is-active' : ''}`} key={project.id} onMouseEnter={() => activate(index)} onFocus={() => activate(index)} onClick={() => activate(index)} onKeyDown={(event) => handleKeyDown(event, index)} aria-current={active === index ? 'true' : undefined} tabIndex="0"><img src={project.image} alt={project.alt} /><div className="project-panel__veil" /><div className="project-panel__caption"><span>{project.id} / 03</span><div><h3>{project.title}</h3><p>{project.subtitle}</p></div></div>{active === index && <p className="project-panel__description">{project.description}</p>}</article>)}</div> }
